def get_bot_move(board_state: dict, color: str, roll: int) -> int:
    """
    Snakes & Ladders is fully automated upon rolling the dice.
    No decision points are available, so always returns index 0.
    """
    return 0
